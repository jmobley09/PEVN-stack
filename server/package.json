{
  "name": "server",
  "version": "1.0.0",
  "description": "express server for backend",
  "main": "server.js",
  "author": "Joshua Mobley",
  "license": "MIT",
  "scripts": {
    "db:drop": "yarn sequelize-cli db:drop",
    "db:create": "yarn sequelize-cli db:create",
    "db:migrate": "yarn sequelize-cli db:migrate",
    "db:seed": "yarn sequelize-cli db:seed:all"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "helmet": "^6.0.1",
    "morgan": "^1.10.0",
    "pg": "^8.10.0",
    "sequelize": "^6.29.3"
  },
  "devDependencies": {
    "dotenv": "^16.0.3",
    "nodemon": "^2.0.21",
    "sequelize-cli": "^6.6.0"
  }
}
